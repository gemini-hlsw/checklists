<div class="collapse in">
  <button {{bindAttr class="freeText:disabled:enabled :btn :btn-small :pull-left"}} {{action checkAll target="view"}}><i class="icon-ok-sign"></i>&nbsp;all</button>&nbsp;<button {{bindAttr class="freeText:disabled:enabled :btn :btn-small :pull-left"}} {{action checkNone target="view"}}><i class="icon-remove-sign"></i>&nbsp;none</button>
  <label class="input-mini pull-right checkbox inline">
    {{view Ember.Checkbox checkedBinding="freeText"}}
    {{freeText}} Any text
  </label>
    <table class="table table-striped table-hover table-bordered table-condensed">
    <tbody>
      {{#each c in choices}}
        <tr>
          <td>
            <label class="checkbox">
              {{view Ember.Checkbox checkedBinding="c.selected" classNames="checkbox"}}{{c.name}}
            </label>
          </small>
          </td>
        </tr>
      {{/each}}
    <tbody>
  </table>
</div>